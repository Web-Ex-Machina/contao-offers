services:
  wem.offers.send_alert:
    class: 'WEM\OffersBundle\Cronjob\SendAlerts'
    arguments:
      $urlGenerator: '@contao.routing.content_url_generator'
      $notificationCenter: '@terminal42.notification_center'
      $logger: '@logger'
    public: true