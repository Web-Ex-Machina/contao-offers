$(document).ready(function () {
  /*$('.job .apply').bind('click', function(){
    var modalObject = $('#applicationModal').modalFW('get');

    $('#applicationModal input[name="pid"]').val($(this).data('id'));
    $('#applicationModal').find('input[name="code"]').val($(this).data('code'));
    $('#applicationModal').find('input[name="title"]').val($(this).data('title'));

    $('#applicationModal').find('input[name="tstamp"]').val(Math.floor(Date.now() / 1000));
    $('#applicationModal').find('input[name="createdAt"]').val(Math.floor(Date.now() / 1000));
  });*/

});

